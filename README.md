# VS Code robot - Image locators vs. keyboard navigation and OCR

This project contains two robots that create new robots(!) using VS Code.

One of the robots use image locators - basically screenshots of the UI elements - to find things to click on. The other uses keyboard navigation and OCR.